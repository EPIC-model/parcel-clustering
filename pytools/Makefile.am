pytoolsdir = $(bindir)/pytools

# avoid byte-compilation because autoconf 2.69 does not support the package
# "importlib" which replaces the package "imp" that got removed in Python 3.12
nobase_pytools_DATA =                	\
	plot_scaling.py
